{
  "name": "@algo/rule-schema",
  "version": "1.0.0",
  "private": true,
  "description": "Versioned strategy rule-tree schema — single contract shared by the builder UI, backtest engine and live engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
